<svg width="100" height="100" viewBox="0 0 100 100"
     xmlns="http://www.w3.org/2000/svg">
  <!-- Inverted warning triangle (elongated, stroke hardened) -->
  <path d="M 22,18 L 78,18 L 50,88 Z"
        fill="none"
        stroke="black"
        stroke-width="6"
        stroke-linejoin="round"/>

  <!-- Exclamation stem (moved up slightly) -->
  <line x1="50" y1="32" x2="50" y2="58"
        stroke="black"
        stroke-width="6"
        stroke-linecap="round"/>

  <!-- Exclamation dot (moved up slightly) -->
  <circle cx="50" cy="68" r="4" fill="black"/>
</svg>
